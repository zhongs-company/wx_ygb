const config = {};

let isProduction = false;

if (isProduction) {
    //生成环境
} else {
    //开发环境
    config.cdn_image_src = `http://1251097942.cdn.myqcloud.com/1251097942/platform/miniApp/shake_broadcast_images`;
    config.websocket_url = `ws://elric:8080/broadcast`;
}

module.exports = config;